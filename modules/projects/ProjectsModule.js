/**
 * modules/projects/ProjectsModule.js
 *
 * Componente React base del módulo Proyectos & Kanban.
 * Para un tenant con uiOverride, se carga desde modules/overrides/{slug}/
 * via lib/moduleRegistry.js.
 *
 * TODO: implementar UI completa del módulo Proyectos.
 */

export default function ProjectsModule() {
  return null;
}
